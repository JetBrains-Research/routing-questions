Write a CLI tool using Click and requests that fetches stats for a GitHub repo from the GitHub REST API and displays them nicely in the terminal. Given a repo like "owner/repo" it should show: stars, forks, open issues, top 5 contributors (login + commit count), and the 5 most recently closed issues (title + closed date). Accept a --token flag for the GitHub API token to avoid rate limits.
