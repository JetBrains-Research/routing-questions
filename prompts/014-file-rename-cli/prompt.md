I need a CLI tool using Click that batch renames files in a directory. It should support: replacing a substring in filenames, applying a regex pattern with a replacement string, adding a prefix or suffix, and converting filenames to snake_case or kebab-case. Always show a preview of the old → new names before applying and ask for confirmation. Use pathlib. A --dry-run flag should skip the confirmation and just show the preview.
