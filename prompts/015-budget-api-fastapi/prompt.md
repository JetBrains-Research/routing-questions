Build a personal budget tracking API with FastAPI. I want to create budget categories with a monthly limit, log transactions (amount, category, description, date), and query spending per category for a given month including how much of the budget is left. Also an endpoint that returns all categories where spending has exceeded the limit. SQLite with SQLAlchemy for storage, Pydantic for request/response validation.
