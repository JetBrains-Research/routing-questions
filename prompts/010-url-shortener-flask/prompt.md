Build a URL shortener as a Flask app. I should be able to POST a long URL and get back a short code, then visiting /<code> should redirect me to the original URL. Store mappings in SQLite with SQLAlchemy. Track how many times each short URL has been visited and expose a /stats/<code> endpoint that returns the original URL and visit count as JSON. Short codes should be randomly generated 6-character alphanumeric strings.
