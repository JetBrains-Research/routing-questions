Build a simple in-memory key-value store server in Python using the socket and threading modules. The server listens on a TCP port and accepts text commands: SET key value, GET key, DELETE key, and KEYS (returns all keys). Values can be any string. Responses should be human-readable lines. Also write a small client CLI using Click that connects to the server and sends commands. No persistence needed, in-memory only.
