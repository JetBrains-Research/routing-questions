I want a simple static site generator in Python. I write pages as Markdown files with a YAML frontmatter block (title, date, tags). Running `python build.py` should convert all .md files in a content/ directory into HTML pages in an output/ directory using Jinja2 templates. There should be a base layout template, an index page listing all posts sorted by date with their tags, and individual post pages. Use mistune for Markdown and PyYAML for frontmatter parsing.
