I write notes in Markdown and want a CLI tool using Click and the mistune library that converts a .md file to a standalone HTML file. It should wrap the output in a minimal HTML page with a <style> block for basic readable typography (nothing fancy, just font, line-height, max-width). If I pass a directory instead of a file, it should convert all .md files in it. Output files should go next to the source files with the same name but .html extension.
