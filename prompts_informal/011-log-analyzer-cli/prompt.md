I want a CLI tool using Click and pandas that parses Nginx access logs (combined log format) and shows useful stats: total requests, top 10 requested URLs, top 10 client IPs, breakdown of HTTP status codes, and average/max response size. Accept a log file path as argument and optionally a --from and --to flag to filter by date range. Print everything as formatted tables to the terminal.
