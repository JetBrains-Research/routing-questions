Create a simple polling/voting web app with Flask. An admin can create a poll with a question and up to 6 options. Anyone can visit a poll's page and vote for one option (no login, just use a session cookie to prevent double voting per browser). After voting, show the current results as a bar chart using matplotlib rendered as a PNG served inline. Store everything in SQLite with SQLAlchemy.
