I want a small ETL pipeline in Python. It should fetch the latest exchange rates from the open.er-api.com API (free, no key needed), transform the data to compute rates relative to EUR (they default to USD base), and store the results in a SQLite database with a timestamp. Then expose a FastAPI endpoint GET /rates?date=YYYY-MM-DD that returns all rates stored for that date. Schedule the fetch to run every hour using APScheduler running in the background when the FastAPI app starts.
