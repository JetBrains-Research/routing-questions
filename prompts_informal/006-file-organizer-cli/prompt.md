I have a messy downloads folder and I want a CLI tool using Click and pathlib that organizes files in a given directory by moving them into subfolders based on their extension (e.g. images/, docs/, videos/, code/, other/). It should have a --dry-run flag that prints what it would do without actually moving anything. Let me also pass a custom mapping like --map pdf=documents --map py=scripts to override defaults.
